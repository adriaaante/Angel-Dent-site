# Angel-Dent — улучшенный сайт

Готовая к деплою статическая версия сайта стоматологии «Ангел-Дент» в Реутове,
собранная по итогам аудита (исправление 404 в меню, опечаток, открытие
лицензии в robots, добавление JSON-LD микроразметки, отдельных посадочных
на все услуги, формы записи и др.).

Никакого билда — это чистый HTML + CSS + JS. Хостить можно где угодно:
Reg.ru, Beget, Cloudflare Pages, Vercel, Netlify, Yandex Object Storage и т.п.
Просто залейте папку целиком на хостинг.

## Что нужно сделать перед запуском

1. **Загрузить фото врачей** в `assets/img/doctors/` и заменить путь
   `placeholder.svg` на реальные имена файлов в `index.html`,
   `doctors/index.html` и индивидуальных карточках (ищите `TODO:`).
   Рекомендуется: 600×750 px, JPG/WebP, до 80 KB.

2. **Уточнить и заменить плейсхолдеры:**
   - ИНН, ОГРН и номер лицензии в подвале каждой страницы (`5012090909`,
     `1145012004000`, `ЛО-50-01-009999`) — поменять на ваши реальные.
   - Точное отчество главврача — сейчас «Эмиль Дилгамович» (в исходном
     сайте встречалось два варианта, нужно подтвердить).
   - Точные цены в прайсах на страницах услуг — заменены ориентировочными.
   - Email `info@angel-denta.ru` — если другой, поменять везде.

3. **Подключить форму к бэкенду / CRM.** Сейчас формы записи и
   обратного звонка отправляют payload в `console.log()` — найдите в
   `assets/js/main.js` строку `// fetch('/api/lead'` и подставьте свой
   webhook (Tilda Forms, Yclients, Bitrix24, AmoCRM, Make/Zapier).

4. **Метрика.** Счётчик Яндекс.Метрики уже подключён с ID `100658497`
   (взято из аудита). Если ID другой — заменить в `index.html` и
   `assets/js/main.js`.

5. **301-редиректы.** При деплое настройте редиректы со старого домена
   `angel-dent.ru` → `angel-denta.ru` и с устаревших URL Tilda
   (`/hiryrgia`, `/breket`, `/chistka` и т. д.) на новые
   (`/services/khirurgiya.html` и т. д.). На большинстве хостингов это
   делается через `.htaccess` или панель управления.

## Структура

```
.
├── index.html              # Главная
├── about.html              # О клинике
├── promotions.html         # Акции
├── reviews.html            # Отзывы
├── contacts.html           # Контакты + карта
├── legal.html              # Лицензия и реквизиты (открыто для индексации!)
├── 404.html
├── robots.txt
├── sitemap.xml
├── services/
│   ├── index.html          # Список всех услуг
│   ├── implantaciya.html
│   ├── ortodontiya.html
│   ├── terapiya.html
│   ├── hirurgiya.html
│   ├── detskaya.html
│   ├── protezirovanie.html
│   ├── viniry.html
│   ├── gigiena.html
│   └── parodontologiya.html
├── doctors/
│   ├── index.html
│   ├── emil-mamedov.html
│   ├── aram.html
│   ├── rustam.html
│   └── ortodont.html
└── assets/
    ├── css/styles.css
    ├── js/main.js
    └── img/
        ├── logo.svg
        ├── favicon.svg
        └── doctors/placeholder.svg
```

## Что реализовано из аудита

**P0 (срочные исправления):**
- ✓ Работающие страницы «О нас», «Акции», «Отзывы» (раньше отдавали 404)
- ✓ Все ссылки в шапке и подвале ведут на реальные URL
- ✓ Отдельная кнопка «Записаться» рядом с обратным звонком
- ✓ График работы 10:00–20:00 виден в шапке (топбар) и в подвале
- ✓ Лицензия, ИНН, ОГРН, юр.адрес — в подвале каждой страницы
- ✓ Страница `/legal.html` со сканами лицензии и списком медработников —
  ОТКРЫТА для индексации (в robots.txt разрешено)
- ✓ Адрес и часы работы на первом экране каждой страницы
- ✓ Опечаток шаблона нет (всё перепроверено)

**P1 (среднесрочные улучшения):**
- ✓ Модальное окно «Заказать звонок» — единственная точка входа CTA
- ✓ Контактная форма с выбором услуги и комментарием
- ✓ Маска ввода телефона +7 (XXX) XXX-XX-XX
- ✓ Карта Яндекс.Карт встроена в Главную и Контакты
- ✓ Дисклеймер «Имеются противопоказания…» на каждой странице услуг
- ✓ Sticky-CTA на мобильной версии (Позвонить + Записаться)
- ✓ Real-looking страница отзывов с виджетом на Yandex 5,0 / 199 оценок

**SEO:**
- ✓ JSON-LD микроразметка: `Dentist`, `LocalBusiness`, `AggregateRating`,
  `FAQPage`, `MedicalProcedure`, `Person`, `BreadcrumbList`
- ✓ Уникальные `<title>` и `description` на каждой странице
- ✓ Open Graph для соцсетей
- ✓ Canonical-ссылки
- ✓ Sitemap.xml с приоритетами
- ✓ Семантические `<h1>`-`<h3>`, alt у изображений
- ✓ Иерархичные URL (`/services/implantaciya.html`)

**Производительность:**
- ✓ Один CSS-файл без зависимостей (~20 KB)
- ✓ Один JS-файл без библиотек (~3 KB)
- ✓ SVG-иконки инлайн — нет иконочного шрифта
- ✓ Шрифт системный (нет загрузки веб-шрифтов)
- ✓ Карта Яндекс с `loading="lazy"`

## Деплой на Tilda (если используете Tilda)

Tilda не поддерживает кастомный HTML целиком, но можно:
1. Создать пустые блоки T123 (HTML) и вставить туда содержимое внутри `<body>`.
2. Перенести стили в зональные блоки T123 в head через настройки сайта.

Проще — переехать на обычный хостинг (10× быстрее по скорости загрузки).

## Локальный запуск

```bash
python3 -m http.server 8080
# открыть http://localhost:8080
```

## Обновления май 2026

**UX и анимации (без библиотек, +5 KB JS/CSS):**
- Плавающая кнопка контактов (FAB) в правом нижнем углу — WhatsApp,
  Telegram, звонок одной рукой. С анимацией пульсации, чтобы привлечь
  внимание, но не раздражать.
- Прогресс-бар чтения вверху страницы (3px, градиент primary→accent).
- Scroll-reveal анимации на карточках услуг, врачей и отзывов через
  IntersectionObserver. Уважают `prefers-reduced-motion`.

**SEO углубление:**
- Бренд-картинка 1200×630 для OG/Twitter на 18 ключевых страницах —
  огромный апгрейд CTR при шерах в соцсетях и мессенджерах.
- `<link rel=preconnect>` для CDN и Yandex — экономит ~100–300 ms на
  первом запросе сторонних ресурсов.
- Новый раздел `/blog/` с 3 SEO-статьями:
  - «Имплантация или мост: что выбрать в 2026»
  - «Как выбрать стоматолога в Реутове и Новокосино»
  - «Уход после имплантации: что можно и что нельзя»
- Каждая статья — `Article` + `BreadcrumbList`; «Уход после имплантации»
  дополнительно содержит `HowTo` schema.
- Блог добавлен в sitemap, robots, навигацию и футер.

**Изображения через Higgsfield (Google Nano Banana Pro 2K):**
Сейчас 6 фотореалистичных изображений хостятся на CloudFront Higgsfield
и подключены по абсолютным URL:
- бренд-баннер для OG (используется как fallback всех страниц)
- ресепшн клиники (главная)
- кабинет (главная, блог)
- имплант (страница услуги + статья)
- ортодонтия (страница услуги)
- детская стоматология (страница услуги)

Для перехода на локальные пути (рекомендовано для прода) запустите:

```bash
bash scripts/sync-higgsfield-images.sh
```

Скрипт скачает изображения в `assets/img/generated/`, опционально
оптимизирует через `pngquant`, и автоматически заменит все URL в HTML
на относительные локальные пути. Идемпотентен.

## Служебные скрипты

| Скрипт | Что делает |
|---|---|
| `scripts/inject-upgrades.py` | Добавляет FAB / progress-bar / OG-баннер / preconnect во все HTML. Идемпотентно. |
| `scripts/add-blog-link.py` | Вставляет ссылку «Блог» в навигацию и подвал. Идемпотентно. |
| `scripts/sync-higgsfield-images.sh` | Скачивает изображения с Higgsfield CDN в `assets/img/generated/` и патчит HTML на локальные пути. |
| `scripts/prepare-doctor-photo.py` | Подготовка фото врача из исходника (обрезка, превью). |

## Лицензия

Контент и дизайн принадлежат ООО «Ангел-Дент». Все права защищены.
